/** Shared primitive types used across the domain models. */

export type CefrLevel = "A2" | "A2+" | "B1";

export type Difficulty = "easy" | "medium" | "hard";

/** An ISO-8601 date-time string, e.g. "2026-08-13T10:00:00.000Z". */
export type IsoDate = string;
